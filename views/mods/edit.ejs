<h2 class="page-header">Edit Mods</h2>
<form action="/mods/<%= mod.id %>?_method=PUT" method="POST">
<div class="form-row">
  <div class="form-item">
    <label>Name</label>
    <input type="text" name="title" value="<%= mod.title %>" />
    <label class="description">Description</label>
    <textarea name="description" class="textarea-description"><%= mod.description  %></textarea>
<label>Platforms</label>
    <input type="text" name="platforms" value="<%= mod.platforms %>" />
<label>Name</label>
    <input type="text" name="requirements" value="<%= mod.requirements %>" />
  </div>
</div>
  <div class="form-row form-row-end btn-row">
    <a class="btn btn-danger" href="/mods/<%= mod.id %>">Cancel</a>
    <button class="btn btn-primary" type="submit">Update</button>
  </div>
</form>
