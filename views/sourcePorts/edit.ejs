<h2 class="page-header">Edit Author</h2>
<form action="/source/<%= source.id %>?_method=PUT" method="POST">
<div class="form-row">
  <div class="form-item">
    <label>Name</label>
    <input type="text" name="title" value="<%= source.title %>" />
    <label class="description">Description</label>
    <textarea name="description" class="textarea-description"><%= source.description  %></textarea>
<label>Platforms</label>
    <input type="text" name="platforms" value="<%= source.platforms %>" />
<label>Name</label>
    <input type="text" name="requirements" value="<%= source.requirements %>" />
  </div>
</div>
  <div class="form-row form-row-end btn-row">
    <a class="btn btn-danger" href="/source/<%= source.id %>">Cancel</a>
    <button class="btn btn-primary" type="submit">Update</button>
  </div>
</form>
